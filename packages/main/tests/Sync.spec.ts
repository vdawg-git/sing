import { beforeEach, describe, expect, it, vi } from "vitest"
import { syncDirectories } from "@/lib/Sync"

describe("syncDirectories", async () => {
  it("should invoke the prisma client with the corrrect arguments", async () => {})
})
