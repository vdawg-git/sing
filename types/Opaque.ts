import type { Opaque } from "type-fest"

/**
 * The opaque type for the `id`, a `number`.
 */
export type IPlaylistID = Opaque<number, "PlaylistID">

/**
 * The opaque type for the `id`, a `number`.
 */
export type ITrackID = Opaque<number, "ITrack">
